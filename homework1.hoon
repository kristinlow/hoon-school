:: This program converts a numerical input to an urbit name address. 

:: Use the gate rune to get an input of any type from a user as an atom. Bind it to the variable 'input'
|=  input=@                                               ::  1
:: Display the input variable with the urbit name aura cast upon it.
`@p`input

